# MathlibAnnex v0.3.0 source release notes (draft)

This is the private source-release candidate for the intended `v0.3.0` release. It adds the exact 60-module project-independent Rosenberg source closure, a neutral imports-only Project entry, an exact Project Source Manifest, and an independent downstream import check.

The candidate retains Lean `4.32.0-rc1`, Mathlib `360da6fa66c1273b76b6b2d8c5666fd5ac2e3b56`, and the committed lock. Its manual private workflow uses receipt schema v3 and treats root, Mankiewicz, Sphere Rigidity, and Rosenberg imports as independent required checks.

This draft does not claim a public tag, GitHub Release, Card or Catalog publication, Project overview approval, website change, or first-publication date.
